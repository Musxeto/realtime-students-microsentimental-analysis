# Progress Report (2026-04-05)

## Project Context
- Project: Real-time Student Behavior Detection (FYP)
- Model family: YOLOv11 (`yolo11n.pt`)
- Repository: `realtime-students-microsentimental-analysis`
- Branch: `feat/mvp`

## What Was Done Today

### 1) Training Notebook Improvements (`train_model.ipynb`)
- Updated the training pipeline for Google Colab T4 usage.
- Added stronger Colab/T4-oriented defaults in training:
  - `epochs=50`
  - `imgsz=640`
  - `batch=-1` (Ultralytics AutoBatch)
  - `amp=True`
  - `optimizer='auto'`
  - `cos_lr=True`
  - `patience=20`
  - `workers=8`
  - `close_mosaic=10`
  - `seed=42`
- Kept CPU-guard behavior for Colab to avoid accidental long CPU training.
- Added a dedicated testing section after training to evaluate saved best weights.
- Removed live inference section from the training notebook for now.
- Cleaned section flow to: setup -> validate -> train -> test -> visualize.

### 2) Robust Test Cell Enhancements (`train_model.ipynb`)
- Made test cell resilient to runtime resets:
  - Auto-reconstructs missing runtime variables (`RUNS_ROOT`, `RUN_DIR`, `BEST_WEIGHTS`, `DATA_YAML`, `device`, `batch`).
  - Auto-installs `ultralytics` if missing.
- Added clear test artifact reporting and metrics printout.

### 3) New Local Notebook Created (`train_model_local.ipynb`)
- Added local-first notebook with sections for:
  - dependency checks/install
  - local path setup
  - dataset validation
  - optional local training
  - local test evaluation using saved `best.pt`
  - result visualization
- Designed for Windows + venv workflow with CPU fallback.

## Data Used

### Dataset Configuration (`dataset/data.yaml`)
- `train: train/images`
- `val: valid/images`
- `test: test/images`
- `nc: 6`
- `names:`
  - `handrise`
  - `read`
  - `sleep`
  - `stand`
  - `using_electronic_devices`
  - `write`
- Roboflow metadata in yaml:
  - workspace: `steven-r2d9x`
  - project: `student_classroom_behavior`
  - version: `1`
  - license: `CC BY 4.0`

### Observed Split Sizes (from run logs)
- Train scan: `10780` images, `94` backgrounds, `0` corrupt
- Validation (train-time): `1539` images, `4983` instances
- Test (local evaluation): `771` images, `2617` instances

## What Ran on Colab vs Local

### Ran on Google Colab (T4 GPU)
- Full model training run (`classroom_model_v1`) completed.
- Environment captured in run args:
  - CUDA device: `0` (Tesla T4)
  - Data path: `/content/drive/MyDrive/dataset/data.yaml`
  - Project path: `/content/drive/MyDrive/fyp_runs`
- Duration from logs: `50 epochs completed in ~3.265 hours`.
- Outputs saved to Drive and later available locally under `fyp_runs/classroom_model_v1`.

### Ran Locally (Windows, CPU)
- Local notebook setup and validation cells executed.
- Local test evaluation executed from saved `best.pt`.
- Local test output saved to `fyp_runs/classroom_model_v1_test`.

## Artifacts Generated

### Training Artifacts (`fyp_runs/classroom_model_v1`)
- `weights/best.pt`
- `weights/last.pt`
- `results.csv`
- `results.png`
- `confusion_matrix.png`
- `confusion_matrix_normalized.png`
- `labels.jpg`
- batch preview images (`train_batch*.jpg`, `val_batch*_pred.jpg`, etc.)

### Test Artifacts (`fyp_runs/classroom_model_v1_test`)
- confusion matrix and PR/F1/P/R plots
- predicted-vs-label validation images

## Key Results and Findings

### Colab Training (from `fyp_runs/classroom_model_v1/results.csv`)
- Final epoch (50):
  - `precision(B) = 0.79275`
  - `recall(B) = 0.83618`
  - `mAP50(B) = 0.85003`
  - `mAP50-95(B) = 0.51739`
- Peak values observed in `results.csv`:
  - Best `mAP50(B)` around `0.85259` (epoch 45)
  - Best `mAP50-95(B)` around `0.51828` (epoch 48)

### Local Test Evaluation (`train_model_local.ipynb` output)
- Overall test metrics:
  - `precision(B) = 0.8077`
  - `recall(B) = 0.8235`
  - `mAP50(B) = 0.8390`
  - `mAP50-95(B) = 0.5125`
- Class-wise (test):
  - `handrise`: P `0.912`, R `0.908`, mAP50 `0.927`, mAP50-95 `0.604`
  - `read`: P `0.814`, R `0.834`, mAP50 `0.886`, mAP50-95 `0.552`
  - `sleep`: P `0.860`, R `0.935`, mAP50 `0.916`, mAP50-95 `0.541`
  - `stand`: P `0.789`, R `0.801`, mAP50 `0.796`, mAP50-95 `0.410`
  - `using_electronic_devices`: P `0.670`, R `0.578`, mAP50 `0.642`, mAP50-95 `0.404`
  - `write`: P `0.802`, R `0.885`, mAP50 `0.867`, mAP50-95 `0.565`

## Interpretation
- The model has solid overall detection performance for the FYP baseline.
- Strongest classes: `handrise`, `sleep`, `write`.
- Weakest class remains `using_electronic_devices` and, to a lesser extent, `stand`.
- Train-time validation and local test are consistent, indicating stable generalization.

## Issues Encountered and Resolutions
- `NameError: BEST_WEIGHTS is not defined`
  - Cause: runtime reset, variables cleared.
  - Fix: test cell now rehydrates required variables automatically.
- `ModuleNotFoundError: ultralytics`
  - Cause: missing package after runtime/kernel reset.
  - Fix: test cell now auto-installs `ultralytics` when missing.
- Colab session-time and checkpoint concern
  - Confirmed checkpoints were created and persisted (`best.pt`, `last.pt`).

## Files Touched Today
- `train_model.ipynb`
- `train_model_local.ipynb`
- `fyp_runs/classroom_model_v1/*` (artifacts)
- `fyp_runs/classroom_model_v1_test/*` (test artifacts)

## Repro Steps (Current Recommended)
1. For local test only:
   - Keep `dataset/` and `fyp_runs/` in project root.
   - Open `train_model_local.ipynb`.
   - Run cells in order; skip optional local training unless needed.
2. For Colab training continuation/future runs:
   - Keep dataset and runs in Drive-backed paths.
   - Reuse robust train/test cells in `train_model.ipynb`.

## Next Suggested Work
- Improve weaker classes (`using_electronic_devices`, `stand`) by adding more examples and hard cases.
- Evaluate error cases from confusion matrix and false positives.
- Add a fixed-seed comparison run with augmentation tuning for class imbalance.
- Add deployment evaluation script for real-time inference FPS/latency on local hardware.
